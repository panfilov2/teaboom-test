# Тестовое задание — карточка товара Teaboom.ru

## Запуск

```
npm install
npm run dev      # запуск с хот-релоадом (Vite)
npm run build    # сборка в dist/
npm run preview  # локальный просмотр собранного dist/
```
